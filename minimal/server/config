# A copy of the license to this software is located 
# in the working directory of this repository

# Set this to the IP address or hostname of your computer
IP=localhost

# Port can be whatever, but has to match on client side
PORT=1234

# Buffer size (in bytes) that limits the max ammount of data that can be sent
BUFF_SIZE=4096

# 3 quality types: "low" | "med" | "high"
   # Low: Greyscale image + Data-saver + Image-reduction (may result in a "blurry" or "cloudy" stream)
   # Medium: Greyscale image + Data-saver.
   # High: Color image + Data-saver
QUALITY=low

# Interface of the camera you want to use
CAM_INTERFACE=